server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
  servlet:
    context-path: /fivegourp_backstage

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/quanxian?characterEncoding=UTF-8
    username: root
    password: admin

  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp


  redis:
    host: localhost
    port: 6379
    jedis:
      pool:
        max-idle: 300
        max-active: 600
        max-wait: 300
  data:
    mongodb:
      host: h
      port: 27017
      database: 1712a

